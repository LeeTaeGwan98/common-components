import BreadcrumbContainer from "@/components/BreadcrumbContainer";

function Account() {
  return (
    <BreadcrumbContainer breadcrumbNode={<>관리자 / 계정 관리</>}>
      sd
    </BreadcrumbContainer>
  );
}

export default Account;
